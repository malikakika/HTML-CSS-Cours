// Déclaration d'une fonction pour calculer la somme de deux nombres
function additionner(a, b) {
    return a + b;
}

// Appel de la fonction
var resultat = additionner(5, 3);
console.log("Le résultat de l'addition est : " + resultat);
